# 2026-09-19 開発履歴

## 実施内容

- 現在のソースコード、既存ドキュメント、Git状態を調査した。
- ドキュメントを次の構成へ整理した。
  - `overview.md`
  - `design.md`
  - `current.md`
  - `decisions.md`
  - `history/`
- 旧ドキュメントの内容を、新しい役割ごとの文書へ統合した。

## ドキュメント上の方針変更

- コードから確認できる事実と、設計意図・判断理由を区別して記録する。
- 理由を確認できない設計判断は、推測せず`要確認`と記録する。
- 現在の仕様は`design.md`、その日の作業は`history/`に記録する。

## 調査で確認した課題

- ローカルブランチ`main`は`origin/main`より2コミット遅れている。
- 未取り込みコミットの「ポストを最新順に表示」と、現在コードのPost昇順取得に差異がある。

## Git同期

- 未コミットの文書整理と`profiles`結合指定の変更を、`backup-docs-and-profile-query`ブランチへ一時コミットして保護した。
- `main`を`origin/main`の2コミットへfast-forward更新した。
- `src/app/page.tsx`の競合を解消した。
- プロジェクトオーナーの指示により、PostとCommentの表示順は新しい順に統一した。
- `src/app/page.tsx`に未コミットの変更がある。
- コメント送信時のフォーム初期化処理には、追加の動作確認が必要な可能性がある。

## 動作確認

- 今回はドキュメント整理のみを行い、アプリのコード変更および動作テストは行っていない。
