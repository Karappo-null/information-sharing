# 2026-09-24 開発履歴

## 検索UIの再構成

- PCのヘッダーとスマートフォンの下部固定バーで、同じ検索入力コンポーネントと検索語の状態を使用するようにした。
- `/search`は検索結果専用の画面にし、ページ内にあった専用検索入力欄を削除した。
- 検索語は画面状態として保持し、`/search?q=...`は初期表示、再読み込み、共有時の検索語復元のために同期するようにした。
- 400msの待機、IME変換中の検索抑止、変換確定後の自動検索、空欄時の結果クリアを共通の処理にまとめた。
- 検索対象、Supabaseへの検索クエリ、検索結果カード、DB、RLSは変更していない。

## 確認

- `npm run lint` と `npm run build` は成功した。
- ブラウザでのPC・スマートフォン操作と、Supabaseに接続した検索結果の手動確認は残っている。
