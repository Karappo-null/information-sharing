# 現在の開発状態

最終更新: 2026-09-25

## 現在実装済みの機能

- メールアドレス・パスワードによる登録とログイン
- 表示名の登録と画面表示
- Topicの作成・作成者本人または管理者による編集・管理者による削除
- Postの作成・投稿者本人または管理者による編集・削除
- Postへの画像添付・削除・拡大表示
- Post編集画面での画像追加、削除予定、保存前プレビューの取り消し
- Postへの一段階コメント・編集・削除
- `member` / `admin` による権限分けとRLS
- SupabaseのDB、Storage、RLSを作成するSQL

## 現在作業中の内容

- Topic編集を作成者本人または管理者に限定した。Post画像の拡大表示と、Post編集時の画像追加・削除予定も実装済みで、ブラウザとSupabaseに接続した手動確認待ち。

## 判明している不具合

### 確認済み

- Comment送信後、非同期保存の完了時点でフォーム参照が`null`になり、一覧再取得前に例外となる不具合があった。2026-09-20に修正した。
- Post・Commentのプロフィール結合結果を配列として参照していたため、表示名を取得できず「メンバー」と表示される不具合があった。2026-09-20に修正した。

### 要確認

- 画像アップロード・削除、RLS、Supabase Auth設定を現在のSupabaseプロジェクトでどこまで検証済みかは、ソースコードからは判定できない。

## 現在の課題

- 2026-09-19に`origin/main`の未取り込み2コミットを取り込み、PostとCommentの表示順を新しい順に統一した。取り込み前の変更は`backup-docs-and-profile-query`ブランチにも保存されている。
- 画像のStorageファイル削除は画面操作で行う。ネットワーク失敗時に孤立ファイルが残る可能性への対策は未実装。
- Topic更新と、管理者による他ユーザーPostへの画像追加に必要なRLS変更を`supabase/schema.sql`へ反映した。現在使用中のSupabaseプロジェクトへの適用状況は未確認。

## 次に行う予定の作業

Topic編集権限とPost画像機能をブラウザとSupabaseに接続して確認し、Topic更新・管理者画像追加のRLSをSupabaseへ適用する。

## 確認が必要な事項

- Supabase DashboardのAuth設定が文書どおりか
- 新規登録を現在も受け付けるか、停止するか
- 現在のSupabaseプロジェクトで画像・コメントを含む一連の動作を確認済みか
- `profiles(...)`関係指定の変更をSupabase上で確認済みか
- Topic更新・管理者向けの`post_images`追加ポリシーを適用済みか
