# 現在の開発状態

最終更新: 2026-09-19

## 現在実装済みの機能

- メールアドレス・パスワードによる登録とログイン
- 表示名の登録と画面表示
- Topicの作成・編集・管理者による削除
- Postの作成・投稿者本人または管理者による編集・削除
- Postへの画像添付・削除
- Postへの一段階コメント・編集・削除
- `member` / `admin` による権限分けとRLS
- SupabaseのDB、Storage、RLSを作成するSQL

## 現在作業中の内容

- このドキュメント構成への整理。
- コード上では、作業中であることを示す機能変更は確認できない。

## 判明している不具合

### 確認済み

- リポジトリ内の情報だけでは、現在未解決の不具合は確認できない。

### 要確認

- `saveComment` は非同期処理の後に `event.currentTarget.reset()` を呼んでいる。Post作成で以前発生したフォーム参照エラーと同種の問題が、コメント送信時にも起きる可能性がある。実際に再現するかは未確認。
- 画像アップロード・削除、RLS、Supabase Auth設定を現在のSupabaseプロジェクトでどこまで検証済みかは、ソースコードからは判定できない。

## 現在の課題

- 2026-09-19に`origin/main`の未取り込み2コミットを取り込み、PostとCommentの表示順を新しい順に統一した。取り込み前の変更は`backup-docs-and-profile-query`ブランチにも保存されている。
- `src/app/page.tsx` はPostとComment取得時に`profiles(...)`という関係指定を使用する。以前の名前付き外部キー指定から変更された理由と、Supabase上での検証状況は要確認。
- 画像のStorageファイル削除は画面操作で行う。ネットワーク失敗時に孤立ファイルが残る可能性への対策は未実装。

## 次に行う予定の作業

明示的に決定・承認された次の機能作業は、リポジトリ内の資料からは確認できない。

候補として上がっている未決定事項は [design.md](./design.md#未決定事項要確認事項) に記録する。次の作業は、ChatGPTでの設計相談とプロジェクトオーナーの承認後に決定する。

## 確認が必要な事項

- Supabase DashboardのAuth設定が文書どおりか
- 新規登録を現在も受け付けるか、停止するか
- 現在のSupabaseプロジェクトで画像・コメントを含む一連の動作を確認済みか
- `profiles(...)`関係指定の変更をSupabase上で確認済みか
