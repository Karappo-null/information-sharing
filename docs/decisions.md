# 設計判断の記録

最終更新: 2026-09-19

## 記録ルール

ここには、コードまたは既存文書から明確に確認できる「決定」だけを記録する。理由が記録されていない場合は推測せず、`要確認` とする。

## Next.js、Supabase、Vercelを使用している

決定:

- Next.jsとSupabaseを使用している。
- READMEではVercelを公開先として想定している。

確認元:

- `package.json`
- `README.md`

理由:

- 要確認。技術選定の相談記録はリポジトリ内にない。

## Supabase Authを使用している

決定:

- 認証にSupabase Authのメールアドレス・パスワード方式を使用している。

確認元:

- `src/app/page.tsx`
- `README.md`

理由:

- 要確認。ほかの認証方式を採用しなかった理由は記録されていない。

## ログイン必須の共有範囲

決定:

- 未ログインではアプリの内容を表示しない。
- ログイン済みユーザーはすべてのTopic、Post、Comment、画像を読める。

確認元:

- `src/app/page.tsx`
- `supabase/schema.sql` のRLS
- 旧仕様文書

理由:

- 要確認。公開範囲をこの形にした背景は記録されていない。

## 管理者ロールを持つ

決定:

- `profiles.role` に `member` と `admin` を保存する。
- 管理者はTopic削除と、他ユーザーのPost・Comment・画像の編集または削除ができる。
- 初期管理者はSQL Editorから手動で設定する。

確認元:

- `supabase/schema.sql`
- `README.md`
- 旧仕様文書

理由:

- 要確認。管理画面を作らずに手動設定とした理由は記録されていない。

## Topic、Post、Commentの権限

決定:

- ログイン済みユーザー全員がTopicを作成・編集できる。
- Topic削除は管理者のみ。
- PostとCommentの編集・削除は、作成者本人または管理者。

確認元:

- `supabase/schema.sql` のRLS
- `src/app/page.tsx`
- 旧仕様文書

理由:

- 要確認。各権限の理由は記録されていない。

## 画像をPostへ添付する

決定:

- Postには画像を最大4枚添付できる。
- JPEG、PNG、WebPを入力として受け付け、ブラウザ内でWebPへ変換して保存する。
- Storage bucketは非公開とする。

確認元:

- `src/lib/image.ts`
- `src/app/page.tsx`
- `supabase/schema.sql`
- 旧仕様文書

理由:

- 要確認。枚数、形式、縮小方式、非公開Storageを選んだ理由はリポジトリ内に記録されていない。

## 一段階のコメントを使用する

決定:

- CommentはPostに紐づく。
- Commentへの返信を表す親Commentは実装しない。

確認元:

- `comments`テーブルと`src/app/page.tsx`
- 旧仕様文書

理由:

- 要確認。入れ子返信を採用しなかった理由は記録されていない。

## PostとCommentを新しい順に表示する

決定:

- PostとCommentは作成日時の新しいものを先に表示する。

確認元:

- プロジェクトオーナーからの2026-09-19の指示
- `src/app/page.tsx`

理由:

- プロジェクトオーナーが自ら変更したと記録されている。詳細な理由は要確認。

## 開発の役割分担

決定:

- ChatGPTが要件整理・設計相談・仕様決定を担当する。
- Codexがコード調査・実装・テストを担当する。
- GitHub上のMarkdownを確定した知識の保存先とする。

確認元:

- プロジェクトオーナーからの2026-09-19の指示

理由:

- プロジェクト運用上の明示的な方針。

## 今後の追記方法

ChatGPTで設計を確定した際は、少なくとも次の情報を追加する。

- 決定内容
- 決定日
- 理由
- 関連する仕様または画面
- 実装済みか、未実装か
